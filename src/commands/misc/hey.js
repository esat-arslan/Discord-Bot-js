module.exports = {
    name: 'hey',
    description: 'hey`s',
    //devOnly: Boolean
    //testOnly: Boolean,
    //options: Object[],

    callBack: (client, interaction) => {
        interaction.reply(`Nope`);
    },
};